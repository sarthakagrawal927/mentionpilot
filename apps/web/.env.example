# Better Auth (D1-backed — see wrangler.toml for D1 binding)
BETTER_AUTH_SECRET=    # Generate with: openssl rand -base64 32
BETTER_AUTH_URL=       # e.g. https://mentionpilot.pages.dev
AUTH_GOOGLE_ID=        # Google OAuth client ID
AUTH_GOOGLE_SECRET=    # Google OAuth client secret

# API
NEXT_PUBLIC_API_URL=https://mentionpilot-api.sarthakagrawal927.workers.dev

# SaaS Maker widgets (feedback / changelog / testimonials)
NEXT_PUBLIC_SAASMAKER_API_KEY=
